#Python list are mutable objects
#We caan create a list by enclosing a swquence in big brackets []

#We xan also create a list using() built-in function

a= [] # An empty list
b=  list() # An empty list using list() function

a= [1, 2, 3] # Non empty list
# In this list, all data are of integer data type But list can also have hetrogenous type

a= [1, "Hello", 2.1, {1, 2, 3}, {"a": 1, "b": 2}]
#In this list data are of diffrent types which is also supported by a python list

# We can use buint-in type() funtion to check the type of an object
type([1,2,3,]) #list
type((1, 2, 3)) #tuple
type(1) #int

#and so on..
